SigmaCartographer
{
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 1024// #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 1 // #{zoom_level}
    leaflet = false
  }
  
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 2048 // #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 2 // #{zoom_level}
    leaflet = false
  }
  
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 4096 // #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 3 // #{zoom_level}
    leaflet = false
  }
  
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 8192 // #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 4 // #{zoom_level}
    leaflet = false
  }
  
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 16384 // #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 5 // #{zoom_level}
    leaflet = false
  }
  
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 32768 // #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 6 // #{zoom_level}
    leaflet = false
  }
  
  Maps
  {
    body = Eve // #{body}
    biomeMap = false
    colorMap = false
    heightMap = false
    normalMap = true // #{options.styles.include?("Normal").to_s}
    oceanMap = true #{options.styles.include?("Ocean").to_s}
    slopeMap = false
    satelliteBiome = true // #{options.styles.include?("Biome").to_s}
    satelliteHeight = true // #{options.styles.include?("Height").to_s}
    satelliteMap = true // #{options.styles.include?("Map").to_s}
    satelliteSlope = true // #{options.styles.include?("Slope").to_s}
    oceanFloor = true
    width = 65536 // #{512 * 2**zoom_level}
    tile = 256
    exportFolder = 7 // #{zoom_level}
    leaflet = false
  }
}
